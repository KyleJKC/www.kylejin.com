<script>
  import { getLocalTheme, getSystemTheme, changePageTheme } from '@/utils/theme'

  function load() {
    const localTheme = getLocalTheme()
    const systemTheme = getSystemTheme()
    changePageTheme(localTheme === 'system' ? systemTheme : localTheme)
  }

  load()
</script>
